#include <bits/stdc++.h>
using std::cout;
using std::cin;
using std::vector;

int main()
{
    vector<vector<int>> T;

    int a;
    while (cin >> a)
    {
        vector<int> row;

        for (int i = 0; i < )
    }
}

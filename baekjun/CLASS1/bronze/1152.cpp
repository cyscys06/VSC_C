#include <bits/stdc++.h>
using std::cout;
using std::cin;
using std::string;

int main()
{
    string a;
    int count = 0;

    while (cin >> a)
    {
        count++;
    }

    cout << count;

    return 0;
}
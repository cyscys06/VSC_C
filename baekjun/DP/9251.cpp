#include <bits/stdc++.h>
using namespace std;
string a, b;
int LCS = 0;

int main()
{
    cin >> a >> b;
    int DP[a.size() + 1][b.size() + 1];

    for (int i = 0; i < a.size() + 1; i++)
    {
        for (int j = 0; j < b.size() + 1; j++)
        {
            if ()
        }
    }

}